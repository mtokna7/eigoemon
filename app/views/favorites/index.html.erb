<p class="mx-auto text-4xl text-center font-rocknroll break-words mt-24"><%= t('main.noted_words_index') %></p>
<% if @favorites.present? %>
  <p class="mx-auto text-2xl text-center font-yusei break-words text-rose-900 mt-3">※ <%=t('main.sound_notice') %></p>
<% end %>

<div class="mockup-code  mt-5 mx-28 bg-gray-600 text-white p-3 pb-52 mb-52">
  <% if @favorites.present? %>
    <%= render partial: "favorite", collection: @favorites  %>
  <% else %>
    <p class="text-2xl px-3 text-center"><%= t('main.not_noted')%></p>
  <% end %>
</div>
